%% Classroom Report — LangGraph pipeline (classroom_report/graph.py)
flowchart TB
    files["Slides + Excel uploads"]
    LC["load_context — reads agent.md and skills.md"]
    AN["analytics_agent — parse files, scores, tiers, charts, top 5"]
    SM["summary_agent — Ollama topic summary, Word"]
    HW["homework_agent — Ollama homework + reviewer validation, Word"]
    BD["badge_agent — Ollama quotes, top 5 PDF badges"]
    END(["END"])

    files --> LC
    LC --> AN
    AN -->|ingest_ok| SM
    AN -->|errors / missing inputs| END
    SM --> HW
    HW --> BD
    BD --> END
